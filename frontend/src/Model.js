import React from 'react'

export default function Model(props){
    let color;
    // if (props.status=="ready"){
    //     color=Green;
    // }
    return (
        <div>
            {props.date}
        </div>
    )
}